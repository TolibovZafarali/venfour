# Copy this file to .env.staging.local or provide the values through the
# staging build environment. Every value below is public browser configuration.
# Never place service-role or provider secrets in a VITE_ variable.

# The Worker serves the browser and API on one origin, so this must stay empty.
VITE_API_BASE_URL=

VITE_STAGING_ORIGIN=
VITE_SUPPORT_EMAIL=
VITE_SUPABASE_URL=https://bjvsgaqitehtwasugvla.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_TURNSTILE_SITE_KEY=
